# פרוטוקול מחקר — הערכת רעיון מסחר חדש

**גרסה:** 1.1 | **נכתב:** 7 באוגוסט 2026 | **עודכן:** 8 באוגוסט 2026
**נובע ישירות מ:** Test 15, מחקר האופציות (Test 14), בדיקת H1, ובדיקת H2.

הפרוטוקול הזה הוא הנכס המרכזי של הפרויקט. חמש בדיקות עצמאיות
הסתיימו בשלילה; מה שנשאר הוא **תשתית מדידה עם תהליך שמונע
מהרעיון הבא להתבשל חודשיים**.

**עדכון 1.1:** נוסף שלב 0 (Economic hurdle) בעקבות H2 — חישוב
שלוקח עשר דקות והכריע את ההשערה לפני כל קוד, נתונים, או Paper.

---

## עיקרון-על: בדיקה שלא הוכח שהיא נכשלת על הקוד השבור אינה בדיקה

זהו כלל שחל **בכל פעם שנכתבת בדיקה** — בשלב 3.5 כשמאמתים כיסוי
קריטריונים, אחרי כל תיקון באג, ובכל בדיקת רגרסיה. אינו שלב שעוברים
פעם אחת ברצף, ולכן הוא כאן בראש ולא כשלב 10.

כל בדיקת רגרסיה חייבת לעבור **mutation check**: להחזיר את הבאג,
לוודא שהבדיקה נכשלת, ואז לשחזר. שימו לב למלכודות שהופכות בדיקה
לחסרת-שיניים (למשל: הרצת המנוע על תיקייה מחוץ ל-repo גורמת ל-`git
status` להחזיר תמיד נקי, כך שבדיקת `dirty` עוברת בכל מצב).

**ראיה מהפרויקט:** `test_forward_engine.py` עבר 7/7 גם עם באג
ה-`dirty` וגם בלעדיו. `test_smartpassive_audit.py` עבר 11/11 ולא
כיסה את פער C2. בשני המקרים הבדיקות היו ירוקות והקוד היה שבור.

---

## תשעת השלבים (כולל 0 ו-3.5)

### 0. Economic hurdle — לפני כל מסמך השערה

**נוסף בעקבות H2 (8.8.2026).** התובנה: המבחן שהכריע את H2 היה
חישוב שלוקח עשר דקות. אפשר היה להריץ אותו **לפני** שנכתב
`H2_hypothesis.md`, לפני fetch, לפני audit, לפני snapshot.

**הכלל:** לפני שכותבים מסמך השערה, לפני שורת קוד, לפני הורדת
נתונים — לבדוק אם הרעיון **יכול** לעבוד:

```
expected_gross = effect_size_best_literature × σ_instrument
net = expected_gross − commission − slippage_realistic
annual = net × trades_per_year
```

אם `annual` < תשואה חסרת סיכון על גודל החשבון — **ההשערה
נדחית כאן.** בלי מסמך, בלי נתונים, בלי קוד.

**דרישות:**
- `effect_size`: האומדן **הטוב ביותר** שהספרות מדווחת (לא ממוצע,
  לא שמרני — הטוב ביותר). אם אין ספרות, אין בדיקה.
- `σ_instrument`: אומדן ex-ante ממקור חיצוני/גנרי, או חישוב
  מנתונים זמינים (לא מנתוני הבדיקה העתידיים).
- `slippage_realistic`: הערכה שמרנית (2+ טיקים הלוך-חזור למיקרו).
- `trades_per_year`: תדירות צפויה מהמנגנון.
- הסף: תשואה חסרת סיכון × גודל חשבון.

**אם הרעיון לא עובר את ה-hurdle:** אין טעם להשקיע ב-preregistration,
קוד, נתונים, או Paper. הרעיון מת. זה חוסך שבועות.

**אם הרעיון עובר:** ממשיכים לשלב 1. המעבר אינו אישור — רק אישור
ששווה לבדוק.

### 1. מנגנון לפני תצפית
השערה חייבת להיגזר ממנגנון כלכלי/מיקרו-מבני שניתן לניסוח, לא
מהתבוננות בתוצאות. H1 נגזרה מצורת-U של נפח תוך-יומי — עובדה
מבנית. זה מה שהפך אותה לבדיקה ראויה גם כשהופרכה.

### 2. Preregistration לפני מגע בתוצאות
מסמך `Hn_hypothesis.md` נכתב **לפני** ההרצה וכולל:
- ההשערה והמנגנון
- תחזיות מספריות
- קריטריוני הפרכה מפורטים
- מגבלות ידועות מראש (גודל מדגם, חלון, מקור נתונים)
- חתימה: "כל שינוי אחרי ראיית התוצאות פוסל את הבדיקה"

### 3. נעילה
Universe, חלון זמן, מקור נתונים, עלויות, פרמטרי אסטרטגיה, ספים —
הכל ננעל במסמך. שינוי סף/חלון/מקור נתונים **אחרי** ראיית תוצאה =
עצירה ודיווח, לא תיקון שקט.

### 3.5. Criteria-to-code audit — לפני ההרצה
הלקח מ-H1: שניים מארבעת קריטריוני ההפרכה לא נאכפו בקוד, ורק
בדיקה ידנית הצילה את הבדיקה מ-false approval (פער +1.484R היה
נקרא "אישור" אילו רק קריטריונים 1-2 היו מחוברים).

דרישות:
- לכל קריטריון במסמך יש מזהה יציב (`Hn-C1`, `Hn-C2`, ...)
- לכל מזהה יש מימוש מפורש בקוד הבדיקה
- manifest/audit מוודא כיסוי דו-כיווני: אין קריטריון ללא מימוש,
  ואין כלל החלטה בקוד שלא נרשם במסמך
- בדיקת mutation: היפוך מכוון של כל תנאי חייב לשנות את פסק הדין
- הכיסוי נבדק **לפני** הורדת/טעינת נתונים

### 4. הספק ודיוק מראש
לחשב לפני ההרצה: SE צפוי של המדד, רוחב רווח סמך, וכמה עסקאות
נדרשות כדי לגלות אפקט בגודל X. אם המדגם חסר הספק — הבדיקה
יכולה רק **לשלול**, וזה נכתב מראש. (ב-H1: SE≈0.5R לפער, כצפוי.)

### 5. Snapshot ו-hash
- נתונים יורדים פעם אחת ונשמרים (parquet/csv)
- hash (SHA-256) של מסמך ההשערה, קונפיגורציה נעולה, וקוד הבדיקה
- הרצה חוזרת חייבת להשתמש באותו snapshot

### 6. הרצה אחת + audit trail
הרצה יחידה; פלט נשמר (summary.json, trades, console). אין "עוד
הרצה עם טוויק קטן".

### 7. פסק דין בינארי
- **הופרכה** → עוצרים. אין H2/H3 על אותם נתונים (ריבוי בדיקות:
  תחת 56 ניסויים הרעש הטוב ביותר מגיע ל-t≈2.32).
- **לא הופרכה** → "מצדיק המשך בדיקה על נתונים ארוכים יותר".
  **זה לא אישור ליתרון.**

### 8. OOS חדש וארוך יותר
רק אם (7) עבר: חלון זמן חדש, רצוי מקור נתונים עצמאי, עם
preregistration משלו. אין לחזור על אותו חלון.

---

## תבנית מסמך השערה

```markdown
# השערה Hn — <שם>
**נרשם מראש:** <תאריך> | **סטטוס:** טרם נבדק

## המנגנון (למה זה לא התאמה לנתונים)
## תחזיות מספריות
## קריטריוני הפרכה (ממוספרים: C1, C2, ...)
## מגבלות שידועות מראש (כולל חישוב הספק)
## חתימה
```

## תבנית קוד בדיקה

```python
CRITERIA = {
    "C1": {"desc": "...", "threshold": ..., "direction": ...},
    "C2": {...},
}

def audit_criteria():
    """assert דו-כיווני: כל קריטריון ממומש, כל מימוש רשום"""

def run():
    audit_criteria()          # ← נכשל כאן אם חסר כיסוי
    data = load_snapshot()    # ← לא יורדים לפני audit
    ...
    for cid, c in CRITERIA.items():
        results[cid] = evaluate(c, data)
    verdict = all_pass(results)
```

---

## כללי ברזל

1. **כלכלה לפני סטטיסטיקה** — בלי מנגנון, אין בדיקה.
2. **נטו אחרי עלויות היא האמת היחידה.**
3. **ברירת המחדל היא overfitting** — כל תוצאה חיובית חשודה עד
   שתעבור OOS.
4. **הבדיקה יכולה רק לשלול** כשהמדגם קטן — וזה בסדר. שלילה
   היא מידע.
5. **שינוי אחרי תוצאה = פסילה.** גם אם הוא "מוצדק בזמן אמת".
   שבעה סבבי כיול לימדו שכל תיקון נראה מוצדק בזמן אמת.

---

## סטטוס נוכחי (אוגוסט 2026)

| בדיקה | תוצאה |
|---|---|
| Tests 1-13 (מומנטום/low-vol/leverage/rotation) | שלילי — פסיבי מנצח |
| Test 14 (אופציות income) | שלילי לחשבון $5k-$50k |
| Test 15 (56 קונפיגורציות מיקרו futures) | שלילי — גרוע מאקראי |
| H1 (חלון נזילות תוך-יומי) | **הופרכה** (קריטריונים 3,4) |
| H2 (חלון סגירה / intraday momentum) | **הופרכה** (FC1: נטו שנתי −$66 < $175) |

**המשמעות:** אין כרגע אסטרטגיה פעילה. חמישה קווי מחקר נסגרו,
כולם מאותה סיבה: עלויות ביחס לגודל הפוזיציה בחשבון $5,000.
תשתית המדידה (בקטסט, סקרינר ×15, לייב עם fail-closed, פרוטוקול זה)
מוכנה ומחכה להשערה הבאה — שתעבור קודם את שלב 0 (Economic hurdle).
